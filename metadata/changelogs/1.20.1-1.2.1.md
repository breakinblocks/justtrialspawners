# Just Trial Spawners 1.20.1-1.2.1

## Hotfix

- Fixes `NoSuchFieldError: CREATIVE_MODE_TAB` crash on mod load
  - The 1.2.0 jar shipped without reobfuscation, so vanilla references and were left under Parchment names instead of being remapped to the SRG names production Forge uses at runtime
  - This release is a clean build with the correct mappings
